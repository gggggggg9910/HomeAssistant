# Core audio processing
pyaudio>=0.2.13
numpy>=1.21.0
sounddevice>=0.4.6

# Speech processing (sherpa-onnx already installed on system for KWS)
# sherpa-onnx>=1.10.0

# Alibaba AI models (ModelScope ecosystem)
# Note: SenseVoice requires FunASR packages
# pysptk is NOT needed for SenseVoice (it's for TTS, not ASR)
# Install with: pip install funasr funasr-runtime funasr-modelscope
modelscope>=1.20.0
# FunASR packages for SenseVoice (required, install separately)
# Uncomment these if modelscope[audio] fails:
# funasr>=1.0.0
# funasr-runtime>=1.0.0
# funasr-modelscope>=1.0.0
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0
scipy>=1.7.0
librosa>=0.9.0
soundfile>=0.10.0
transformers>=4.21.0
# datasets version must be compatible with modelscope
datasets>=2.4.0,<3.0.0

# Alibaba AI models - additional dependencies
oss2>=2.17.0
addict>=2.4.0
openmim>=0.3.0
timm>=0.9.0
simplejson>=3.17.0
sortedcontainers>=2.4.0

# Alibaba Qwen LLM
dashscope>=1.14.0

# Text-to-speech (Piper TTS - recommended for Raspberry Pi)
# Fast, lightweight neural TTS optimized for embedded devices
piper-tts>=1.2.0

# Configuration and utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async and concurrency
aiohttp>=3.8.0
asyncio-mqtt>=0.13.0

# Logging and debugging
colorlog>=6.7.0

# Additional audio processing
resampy>=0.4.0
pydub>=0.25.0